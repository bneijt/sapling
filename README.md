# Sapling: small media server

Simple, (mostly) read-only media hosting.

- Rely on the browser to support media playback
- Avoid indexing and state
- Serve audio and video through the same simple browser UI
- Thumbnail like Freedesktop, to avoid multiple indexes of thumbnails
